.git
.github
target
logs
perf
docs

# The image is built from src/, which already contains the compiled UI bundle,
# so the frontend sources and their dependencies are not needed in the context.
ui
node_modules
